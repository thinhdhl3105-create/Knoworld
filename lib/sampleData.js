// Sample/placeholder content used ONLY when Supabase is not configured.
// All placeholder case studies / videos have been cleared so the platform
// starts empty and ready for real uploads. Once Supabase is configured the
// app reads live data and these arrays are never used.

export const sampleContent = [];

export const sampleByKind = (kind) => sampleContent.filter((c) => c.kind === kind);

export const sampleConcepts = [];
export const sampleConceptLinks = [];
export const sampleFrameworks = [];
